import React from 'react';
import { Typography, Box } from '@mui/material';

const Roles = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">Gestión de Roles</Typography>
      <Typography variant="body1">Aquí puedes definir y asignar los roles del sistema.</Typography>
    </Box>
  );
};

export default Roles;
