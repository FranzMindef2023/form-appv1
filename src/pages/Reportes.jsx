import React from 'react';
import { Typography, Box } from '@mui/material';

const Reportes = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold">Generación de Reportes</Typography>
      <Typography variant="body1">Aquí puedes generar reportes del sistema.</Typography>
    </Box>
  );
};

export default Reportes;
